import { Add } from "iconsax-react";

const AddButton = () => {
    return (
        <div 
            className="rounded-[50%] text-white w-[32px] h-[32px] bg-color6 flex items-center justify-center"
        > 
            <Add size="24" color="#FFF"/> 
        </div>
    );
}
 
export default AddButton;