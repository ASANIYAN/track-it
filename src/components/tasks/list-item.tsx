import move_task_icon from "@";

const ListItem = () => {
  return (
    <section className="flex items-center justify-between">
      <section></section>
    </section>
  );
};

export default ListItem;
